// jshint esversion: 6
var name = 'John Doe';

// Concatenating strings with the '+' operator
var greet = 'Hello ' + name;

// Concatenating strings with template literal (available in ES6) using the
// '`' (backtick) operator
var greet2 = `Hello ${name}`;

console.log(greet);
console.log(greet2);
